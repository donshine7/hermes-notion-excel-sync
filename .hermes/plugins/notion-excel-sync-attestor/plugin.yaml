name: notion-excel-sync-attestor
version: "0.6.7"
description: "Consume authenticated Telegram sync, correction, and schema commands with separate exact approval domains."
author: "local"
kind: standalone
provides_hooks:
  - pre_gateway_dispatch
