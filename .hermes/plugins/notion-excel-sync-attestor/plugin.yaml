name: notion-excel-sync-attestor
version: "0.4.1"
description: "Consume authenticated Telegram data-sync and schema-provisioning commands with separate exact approval domains."
author: "local"
kind: standalone
provides_hooks:
  - pre_gateway_dispatch
